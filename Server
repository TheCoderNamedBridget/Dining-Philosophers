

import java.net.*;
import java.util.ArrayList;
import java.io.*;

public class Server
{
	ArrayList<Integer> forks = new ArrayList<Integer>();
  public static void main(String[] args) 
  {
		DiningPhilosopherClient philo1 = new DiningPhilosopherClient(1);
//		DiningPhilosopherClient philo2 = new DiningPhilosopherClient(2);
//		DiningPhilosopherClient philo3 = new DiningPhilosopherClient(3);
//		DiningPhilosopherClient philo4 = new DiningPhilosopherClient(4);
//		DiningPhilosopherClient philo5 = new DiningPhilosopherClient(5);
		
		System.out.println(philo1.waitRandomTime());
		System.out.println(philo1.waitRandomTime());
		//need to create a thread for each philosopher that will try to call the belowe methods
		
		
		//make philosophers wait for a random amount of time between 1-3
		//then call take forks 
		//wait again then call return forks
  }
}
